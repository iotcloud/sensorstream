zk.servers: ["localhost:2181"]
spouts:
    sentence:
        broker: "rabbitmq"
        sensor: "wordcount"
        channel: "sentence"
        fields: ["sentence", "sensorID", "time"]
        properties:
          ackMode: "auto"
bolts:
    command:
        broker: "rabbitmq"
        sensor: "wordcount"
        channel: "count"
        fields: ["count", "sensorID", "time"]
