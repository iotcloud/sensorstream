zk.servers: ["localhost:2181"]
spouts:
    kinect1:
        broker: "rabbitMQ"
        sensor: ""
        channel: ""
        fields: ["", ""]
        messageBuilder:
        properties:
          ackMode: "auto"
    kinect2:
        broker: "rabbitMQ"
        sensor: ""
        channel: ""
        fields: ["", ""]
        messageBuilder:
        properties:
          ackMode: "auto"
bolts:
    command:
        broker: "rabbitMQ"
        sensor:
        channel:
        fields: ["", ""]
        messageBuilder:


